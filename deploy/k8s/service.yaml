apiVersion: v1
kind: Service
metadata:
  name: referralhub
  namespace: referralhub
  labels:
    app.kubernetes.io/name: referralhub
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: referralhub
  ports:
    - name: http
      port: 80
      targetPort: http
